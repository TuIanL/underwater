# Localization Baseline Package

When the first supervised and semi-supervised experiments are complete, store the baseline package in this directory.

Recommended contents:

- `best.pt`: selected checkpoint
- `schema.md`: exported copy of the active keypoint schema
- `metrics.json`: held-out evaluation report
- `failure_modes.md`: experiment-specific notes on persistent failure cases
